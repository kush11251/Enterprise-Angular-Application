// docker compose file
version: '3'
services:
  app:
    build: .
    ports:
      - '4200:4200'
    depends_on:
      - node

  node:
    image: node:18
    working_dir: /app
    volumes:
      - ./:/app
    command: npm install && npm run build && npm run start